[package]
name = "wallet-storage-indexeddb"
version = "0.1.0"
edition = "2021"
license = "SEE LICENSE IN license.md"

[lib]
path = "src/lib.rs"

[dependencies]
wallet-storage = { path = "../wallet-storage", features = ["indexeddb"] }
